import Layout from 'components/Layout'
import Typography from '@material-ui/core/Typography'

const Theme = () => {
  return (
    <Layout title="Theme">
      <Typography component="h3">To update the colors edit <code>theme.js</code> in the root of your project.</Typography>
    </Layout>
  )
}

export default Theme